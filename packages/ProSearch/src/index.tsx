import React from 'react';

export interface ProSearchProps {
  title?: string;
}

export default (props: ProSearchProps) => {
  const { title } = props;

  return (
    <div>2233444444, { title }</div>
  )
}